You are a senior full-stack engineer.

I already have a web-based school management / attendance system, but many buttons do not work and data is not persisting.

Your task is to FIX and COMPLETE the system with the following strict requirements.

-----------------------------------
TECH STACK
-----------------------------------
- Frontend: HTML, CSS, JavaScript (mobile-first, responsive)
- Backend: Firebase (Auth + Firestore)
- Platform: Web app usable on Android phones and tablets
- No TypeScript
- No unnecessary libraries

-----------------------------------
CORE REQUIREMENTS (MANDATORY)
-----------------------------------

1. AUTHENTICATION
- Use Firebase Email/Password authentication
- Each teacher has a unique login
- After login, load teacher profile from Firestore

Firestore structure:
teachers/{teacherId}
{
  name,
  email,
  assignedClass   // example: "8B"
}

Teachers must ONLY access their assigned class.

-----------------------------------

2. DATA STRUCTURE (USE EXACTLY THIS)
Create and use these Firestore collections:

classes/{classId}
{
  classTeacherId
}

students/{studentId}
{
  name,
  rollNo,
  classId,
  status: "active" | "inactive"
}

attendance/{date}/{studentId}
{
  status: "Present" | "Absent" | "Late",
  markedBy,
  timestamp
}

-----------------------------------

3. FIX ENROLL / NEW ADMISSION (CRITICAL BUG)
- When enrolling a new student:
  - Save student data to Firestore (NOT local state)
  - Student must persist when page reloads
- After saving, re-fetch students from Firestore
- Do NOT lose data on navigation

-----------------------------------

4. ATTENDANCE SYSTEM (REPLACE BROKEN FEATURES)
- REMOVE the "sync_server" button completely
- ADD a "SAVE ATTENDANCE" button

SAVE ATTENDANCE behavior:
- Get today’s date automatically
- Loop through all students in class
- Save attendance to:
  attendance/{today}/{studentId}
- Attendance must persist after refresh

-----------------------------------

5. STATUS INPUT FIX (IMPORTANT)
Currently typing "P", "Present", "A", "Absent", "Late" does nothing.

FIX THIS:
- Accept free-text input
- Normalize values:
  - "p", "present" → Present
  - "a", "absent" → Absent
  - "l", "late" → Late
- Update UI immediately
- Save correctly on button click

-----------------------------------

6. BUTTON FIXES (ALL MUST WORK)
Fix or implement functionality for:
- AI Insight Engine
- AI Recommendation
- Decision Suggestions

These do NOT need real AI.
Use logic based on attendance data:

Examples:
- Attendance % per student
- Flag students < 75%
- Suggest parent call if absenteeism detected

NO dead buttons. Every button must show output.

-----------------------------------

7. REMOVE AUDIT LOG COMPLETELY
- Delete audit_log feature
- Remove UI and backend references
- Replace with meaningful attendance summary

-----------------------------------

8. EDIT / DELETE STUDENT
- Add Edit button:
  - Update student name / roll number
- Add Delete button:
  - Do NOT hard delete
  - Set status = "inactive"
- Inactive students:
  - Hidden from daily attendance
  - Old records preserved

-----------------------------------

9. TEACHER-CLASS ISOLATION (SECURITY LOGIC)
- On login:
  - Fetch teacher document
  - Read assignedClass
  - Load ONLY students with matching classId
- Teachers cannot see or modify other classes

-----------------------------------

10. MOBILE OPTIMIZATION (NON-NEGOTIABLE)
- UI must be usable on phones
- Large buttons
- Card-based layout
- No tiny tables
- Touch-friendly controls

-----------------------------------

11. PAGE FLOW
- Login → Teacher Dashboard
- Dashboard shows:
  - Class name
  - Total students
  - Attendance summary
- Class view:
  - Student cards
  - Status input
  - Save Attendance button

-----------------------------------

12. CODE QUALITY
- Use clear function names
- Comment major logic
- No placeholder buttons
- No console-only features
- Everything must work visually

-----------------------------------

FINAL OUTPUT EXPECTED
-----------------------------------
- Fully working attendance system
- All buttons functional
- Firebase fully integrated
- Data persists across reloads
- Teachers can use it on phones
- System ready for real school deployment

Fix existing code where possible instead of rewriting everything.
If something is missing, IMPLEMENT IT.

DO NOT SIMPLIFY.
DO NOT SKIP FEATURES.